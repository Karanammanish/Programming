// Arrays
// Array is also the object but here we no need to apply key name
//let values = new Array(); construct function as we are using keyword;

//another way to add array
let values = []; //empty array
console.log(values);
console.log(values.length); //to know the length of the array

//how to add values in the array

values = [3 , 5 ,6];
console.log(values);
console.log(values.length);
//One way of the adding value

values = [];
//How to add values to the  array
//we can use another method now is push
values.push(5); //only one number can be pushed
console.log(values)

values = [3 , 5 ,6];
//How to fetch the value from the array
//As we dont have the key here
console.log(values[0]); //"[]" bracket are used to fetch the number
