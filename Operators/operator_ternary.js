//Another way other than "if loop"
//For two option only
// "?:" that is ternary operator
// Lets do on even and odd
let num = 5;
let result;
result = num % 2 === 0 ? "Even": "Odd";
console.log(result)